---
title: Update project phase estimate module
---

# Run 20

Replace the `==` marked instructions in this file while you work on the task.

## Context

Read the `AGENTS.md` and `README.md` to get understanding of the project.

## Task

Update the `addons/project/project_phase_estimate` module.

Add new field `allow_estimate​` to `project.project​` with description "Add estimates linked to phases to this project.". Show the field on the project form after `allow_timesheets` (defined in `hr_timesheet`).

If the option is enabled, then the tab project_estimates​ is visible.

If the `invoiced_timesheet​` (defined in `sale_timesheet`) settings configuration is set to validated entries, then the `effective_hours​` cacluation must be filter for validated hours only.


## Worklog

==Fill this in as you work on the task==

## Summary

==Fill this once you completed the task==
