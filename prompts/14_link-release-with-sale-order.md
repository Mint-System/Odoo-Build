---
title: Link release with sale order
---

# Run 14

Replace the `==` marked instructions in this file while you work on the task.

## Context

Read the `AGENTS.md` and `README.md` to get understanding of the project.

## Task

In `addons/kubernetes/sale_helm/models/helm_release.py` convert the `sale_line_ids` to many2one `sale_line_id`.
When `action_confirm` is executed also set `sale_line_id`.
Show `sale_line_id` on the release form below `partner_id`.

## Worklog

==Fill this in as you work on the task==

## Summary

==Fill this once you completed the task==
