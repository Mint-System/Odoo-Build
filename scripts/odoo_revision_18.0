#!/bin/bash

ODOO_REVISION=18.0

# Checkout odoo repo
cd odoo && git checkout 18.0
cd ..

# Checkout enterprise repo
cd enterprise && git checkout 18.0
cd ..

# Checkout theme repo
cd themes && git checkout 18.0
cd ..

# Pull docker image
DIGEST=odoo:18.0
docker pull "$DIGEST"
